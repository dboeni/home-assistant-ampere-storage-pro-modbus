{
  "config": {
    "step": {
      "user": {
        "title": "AmpereStorage.Pro Setup",
        "data": {
          "host": "ip address",
          "name": "name",
          "port": "modbus TCP port",
          "unit": "Unit",
          "scan_interval": "Query the sensors in seconds",
          "scan_interval_long": "Query the longterm data in seconds (multiple of scan_interval)"
        }
      }
    },
    "error": {
      "already_configured": "Device is already configured"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  }
}